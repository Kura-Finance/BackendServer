runtime: nodejs20

env: standard
entrypoint: npm run start

# 環境變數配置
env_variables:
  NODE_ENV: "production"
  # 其他敏感變數通過 Cloud Run 秘密管理器設置:
  # - ENCRYPTION_KEY (AES-256-GCM 密鑰)
  # - JWT_SECRET
  # - PLAID_SECRET
  # - MAILERSEND_API_KEY
  # - DB_PASSWORD
